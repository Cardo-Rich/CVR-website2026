The default container surface — white paper, 24px radius, soft navy shadow. Compose pillars, testimonials, and forms inside it.

```jsx
<Card hover>
  <h3>Transparent accounting</h3>
  <p className="muted">Every dollar visible, no mystery fees.</p>
</Card>
```

Pass `hover` for interactive lift; `padding` to adjust inner spacing.
