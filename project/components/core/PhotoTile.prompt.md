Image tile for properties and decor with the brand's gradient fallback, dark scrim, caption and meta pill. Always degrades gracefully before real photography is dropped in.

```jsx
<PhotoTile img="/la-jolla.jpg" tag="La Jolla Bluff Villa" meta="4 BR" ratio="4 / 3" />
```

Omit `img` to show the navy→teal gradient placeholder; tune it with `phA`/`phB`.
